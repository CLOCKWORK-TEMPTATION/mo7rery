# تقرير تفعيل الملفات الشامل — مشروع أفان تيتر (محرر السيناريو العربي)

> تاريخ التقرير: 2026-02-22
> المحرك: TipTap v3 (ProseMirror) | React 19 | Vite 6 | TypeScript

---

## جدول المحتويات

1. [ملخص تنفيذي](#ملخص-تنفيذي)
2. [ملفات النواة (Core)](#ملفات-النواة)
3. [ملفات الأنواع (Types)](#ملفات-الأنواع)
4. [ملفات الأدوات (Utils)](#ملفات-الأدوات)
5. [جدول التفعيل الشامل](#جدول-التفعيل-الشامل)
6. [مقارنة مع المنافسين](#مقارنة-مع-المنافسين)
7. [توصيات](#توصيات)

---

## ملخص تنفيذي

| الفئة         | مفعّل  | غير مفعّل | المجموع |
| ------------- | ------ | --------- | ------- |
| ملفات النواة  | 4      | 1         | 5       |
| ملفات الأنواع | 9      | 0         | 9       |
| ملفات الأدوات | 12     | 0         | 12      |
| **المجموع**   | **25** | **1**     | **26**  |

> ملف واحد فقط غير مفعّل: `toolbar.ts` — كود قديم (Legacy) تم استبداله بشريط أدوات React.

---

## ملفات النواة

### 1. `src/main.tsx` — نقطة الدخول

| الحقل          | القيمة                                    |
| -------------- | ----------------------------------------- |
| **الحالة**     | ✅ مفعّل                                  |
| **الحجم**      | 61 سطر                                    |
| **الصادرات**   | لا يوجد (ملف تأثيرات جانبية)              |
| **المستهلكون** | `index.html` عبر `<script type="module">` |

**الدور:**
سلسلة إقلاع التطبيق بالكامل:

1. استيراد 7 ملفات CSS بالترتيب (globals → ui-kit → main → page → toolbar → shell)
2. تهيئة `ThemeProvider` (الوضع الداكن افتراضياً، مفتاح التخزين: `filmlane.theme`)
3. إنشاء `Toaster` للإشعارات وإلحاقه بـ `document.body`
4. تركيب `<App />` داخل `<StrictMode>` على عنصر `#app`

**سلسلة الإقلاع:**

```
index.html → main.tsx → ThemeProvider + Toaster + App.tsx
```

---

### 2. `src/App.tsx` — المكوّن الجذري

| الحقل          | القيمة              |
| -------------- | ------------------- |
| **الحالة**     | ✅ مفعّل            |
| **الحجم**      | 1,068 سطر           |
| **الصادرات**   | `App` (مكوّن React) |
| **المستهلكون** | `main.tsx`          |

**الدور:**
الغلاف الرئيسي للتطبيق بالكامل. يدير:

- **الواجهة:** Header (60px) + Sidebar + منطقة المحرر + Floating Dock + Footer
- **اختصارات لوحة المفاتيح:** `Ctrl+0-7` لأنواع العناصر، `Ctrl+S/O/N/Z/Y/B/I/U` للأوامر
- **عمليات الملفات:** فتح، إدراج، حفظ HTML، تصدير، طباعة
- **إحصائيات المستند:** صفحات، كلمات، أحرف، مشاهد (مباشر)
- **نظام الكتابة:** وضع تلقائي (auto-live) ووضع مؤجل (deferred)
- **قائمة الإدراج:** إدراج ديناميكي لعناصر السيناريو مع قوالب
- **6 أقسام قائمة:** ملف، تعديل، إضافة، تنسيق، أدوات، مساعدة

**البنية المعمارية:**

```
App.tsx (React)
├── Header (شعار + قوائم + حالة)
├── Sidebar (بحث + أقسام + بطاقة AI)
├── Main Area
│   ├── Floating Dock Toolbar (14 زر)
│   └── Editor Mount Point → EditorArea (imperative)
└── Footer (إحصائيات + نوع العنصر الحالي)
```

---

### 3. `src/editor.ts` — مصنع المحرر

| الحقل          | القيمة                                                 |
| -------------- | ------------------------------------------------------ |
| **الحالة**     | ✅ مفعّل                                               |
| **الحجم**      | 205 سطر                                                |
| **الصادرات**   | `SCREENPLAY_ELEMENTS`, `createScreenplayEditor`        |
| **المستهلكون** | EditorArea, EditorToolbar, ScreenplayEditor (4+ ملفات) |

**الدور:**
مصنع (Factory) ينشئ محرر TipTap مُهيَّأ بالكامل مع:

- **13 إضافة مسجّلة:**
  - 8 عناصر سيناريو (بسملة، رأس مشهد ×3، حركة، شخصية، حوار، توصيف، انتقال)
  - `ScreenplayCommands` (أوامر مخصصة)
  - `PasteClassifier` (تصنيف تلقائي بالذكاء)
  - `Pages` (ترقيم صفحات A4 — TipTap Pro)
  - أساسيات: Document, Text, Bold, Italic, Underline

- **تنسيق الصفحة:**
  - A4 بدقة 96 PPI: `794px × 1123px`
  - هوامش: يمين 40px، يسار 47px
  - رأس الصفحة: 77px، تذييل مع رقم الصفحة

- **`SCREENPLAY_ELEMENTS`** — مصفوفة ثابتة لـ 8 أنواع عناصر:

| الأيقونة | الاسم          | الاختصار | النوع              |
| -------- | -------------- | -------- | ------------------ |
| ﷽        | بسملة          | Ctrl+0   | basmala            |
| 🎬       | سطر رأس المشهد | Ctrl+1   | sceneHeaderTopLine |
| 📍       | رأس المشهد (3) | Ctrl+2   | sceneHeader3       |
| 📝       | حركة           | Ctrl+3   | action             |
| 👤       | شخصية          | Ctrl+4   | character          |
| 💬       | حوار           | Ctrl+5   | dialogue           |
| 🎭       | توصيف          | Ctrl+6   | parenthetical      |
| 🔀       | انتقال         | Ctrl+7   | transition         |

---

### 4. `src/toolbar.ts` — شريط أدوات قديم

| الحقل          | القيمة                                |
| -------------- | ------------------------------------- |
| **الحالة**     | ❌ غير مفعّل (Legacy / Dead Code)     |
| **الحجم**      | 189 سطر                               |
| **الصادرات**   | `createToolbar`                       |
| **المستهلكون** | ⚠️ لا أحد — لا يوجد استيراد في أي ملف |

**الدور (لو كان مفعّلاً):**
شريط أدوات مبني بـ Vanilla DOM (بدون React) يحتوي:

- قائمة منسدلة لأنواع عناصر السيناريو
- أزرار تنسيق (غامق، مائل، تسطير)
- مؤشر نصي للعنصر الحالي
- مستمعات أحداث على `selectionUpdate` و `transaction`

**سبب وجوده:**

- كان شريط الأدوات الأصلي قبل إعادة تصميم الواجهة بـ React
- تم استبداله بـ:
  1. **Floating Dock** في `App.tsx` (14 زر عائم)
  2. **EditorToolbar** مكوّن React كامل
- التعليق في الملف نفسه يقول:
  > _"شريط Dock في App.tsx هو الشريط النشط حالياً؛ هذا الملف متاح كبديل"_

**لماذا لا يزال موجوداً؟**

- يُستخدم كـ **بديل احتياطي** (fallback) في حالة الحاجة لشريط أدوات بدون React
- ملف CSS الخاص به (`toolbar.css`) لا يزال مستورداً في `main.tsx`

**التوصية:** يمكن حذفه بأمان أو نقله لمجلد `archive/`.

---

### 5. `src/vite-env.d.ts` — تعريفات أنواع Vite

| الحقل          | القيمة                           |
| -------------- | -------------------------------- |
| **الحالة**     | ✅ مفعّل (بنية تحتية)            |
| **الحجم**      | 10 أسطر                          |
| **الصادرات**   | لا يوجد (تعريفات TypeScript فقط) |
| **المستهلكون** | مترجم TypeScript تلقائياً        |

**الدور:**
يوفر تعريفات أنواع TypeScript لميزات Vite:

- `import.meta.env.VITE_*` — متغيرات البيئة
- استيراد الأصول الثابتة (صور، خطوط)
- وحدات CSS
- ملف مولّد تلقائياً من Vite، لا يُعدَّل يدوياً

---

## ملفات الأنواع (Types)

### 6. `src/types/screenplay.ts` — أنواع السيناريو الأساسية

| الحقل          | القيمة                                                         |
| -------------- | -------------------------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                                       |
| **الصادرات**   | `DocumentStats`, `LineType`                                    |
| **المستهلكون** | App.tsx, EditorArea, EditorFooter, paste-classifier (6+ ملفات) |

**الدور:**

- `DocumentStats`: إحصائيات المستند المباشرة (كلمات، أحرف، صفحات، مشاهد)
- `LineType`: اتحاد أنواع 10 عناصر سيناريو (basmala, action, dialogue, ...)
- **الملف الأكثر استخداماً** في مجلد الأنواع

---

### 7. `src/types/file-import.ts` — أنواع استيراد الملفات

| الحقل          | القيمة                                                         |
| -------------- | -------------------------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                                       |
| **الصادرات**   | 8 (أنواع + ثوابت + دوال)                                       |
| **المستهلكون** | App.tsx, ScreenplayEditor, file-picker, extract/\* (10+ ملفات) |

**الدور:**

- `ACCEPTED_FILE_EXTENSIONS`: الامتدادات المدعومة (doc, txt, pdf, fountain, fdx)
- `FileExtractionResult`: نتيجة استخراج النص من ملف
- `getFileType()`: تحديد نوع الملف من اسمه
- `FileImportMode`: وضع الاستيراد (replace / insert)

---

### 8. `src/types/editor-clipboard.ts` — حافظة المحرر المخصصة

| الحقل          | القيمة                                         |
| -------------- | ---------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                       |
| **الصادرات**   | 4 (ثابت MIME + 3 أنواع)                        |
| **المستهلكون** | EditorArea.ts, editor-engine.ts (7+ استخدامات) |

**الدور:**
نظام حافظة مخصص يحافظ على تصنيف الكتل عند النسخ/اللصق:

- `FILMLANE_CLIPBOARD_MIME`: نوع MIME مخصص `application/x-filmlane-blocks+json`
- `EditorClipboardPayload`: هيكل البيانات المنسوخة (كتل + مصدر + طابع زمني)
- عند النسخ: يحفظ تصنيف كل كتلة (حوار، حركة، إلخ)
- عند اللصق: يسترجع التصنيف بدون إعادة تشغيل المصنِّف

---

### 9. `src/types/editor-engine.ts` — واجهة محرك المحرر

| الحقل          | القيمة                 |
| -------------- | ---------------------- |
| **الحالة**     | ✅ مفعّل               |
| **الصادرات**   | 2 (واجهتان)            |
| **المستهلكون** | App.tsx (3+ استخدامات) |

**الدور:**
عقد واجهة (Interface Contract) بين مكونات UI ومحرك TipTap:

- `EditorEngineAdapter`: واجهة موحدة تسمح بتبديل المحرك بدون تغيير UI
- تشمل: insertBlocks, replaceBlocks, getBlocks, runCommand, عمليات الحافظة
- **نمط تصميم Strategy Pattern** لفصل UI عن المحرك

---

### 10. `src/types/structure-pipeline.ts` — خط أنابيب البنية

| الحقل          | القيمة                                                                  |
| -------------- | ----------------------------------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                                                |
| **الصادرات**   | 7 (أنواع + ثوابت + واجهات)                                              |
| **المستهلكون** | structure-pipeline.ts, plain-text-to-blocks.ts, index.ts (8+ استخدامات) |

**الدور:**
سياسات ونتائج خط أنابيب تحويل النص إلى كتل سيناريو:

- `StructurePipelineMergePolicy`: سياسة الدمج (none / safe / aggressive)
- `StructurePipelineClassifierRole`: صلاحيات المصنِّف (label-only / limited-rewrite)
- `ProjectionGuardReport`: حارس الجودة — يمنع الكتابة فوق البنية بنتائج أسوأ
- `DEFAULT_STRUCTURE_PIPELINE_POLICY`: الإعدادات الافتراضية

---

### 11. `src/types/typing-system.ts` — نظام الكتابة التلقائي

| الحقل          | القيمة                    |
| -------------- | ------------------------- |
| **الحالة**     | ✅ مفعّل                  |
| **الصادرات**   | 10 (أنواع + ثوابت + دوال) |
| **المستهلكون** | App.tsx (8+ استخدامات)    |

**الدور:**
نظام التصنيف التلقائي أثناء الكتابة:

- `TypingSystemMode`: 3 أوضاع — plain (يدوي)، auto-deferred (مؤجل)، auto-live (مباشر)
- `TypingSystemSettings`: إعدادات المستخدم (وضع، مدة الخمول، نطاق)
- `DEFAULT_TYPING_SYSTEM_SETTINGS`: القيم الافتراضية
- `sanitizeTypingSystemSettings()`: تنظيف/تصحيح الإعدادات عند بدء التشغيل
- `minutesToMilliseconds()`: تحويل دقائق لمللي ثانية لمؤقت الخمول

---

### 12. `src/types/agent-review.ts` — مراجعة الوكيل الذكي

| الحقل          | القيمة                             |
| -------------- | ---------------------------------- |
| **الحالة**     | ✅ مفعّل                           |
| **الصادرات**   | 5 واجهات                           |
| **المستهلكون** | paste-classifier.ts (6+ استخدامات) |

**الدور:**
أنواع طلب/استجابة لنقطة نهاية `/api/agent/review` التي تستخدم Claude لمراجعة الأسطر المشبوهة:

- يعمل عندما يكون نقاط الاشتباه ≥ 74 مع ≥ 2 مؤشرات
- `AgentReviewRequestPayload`: يرسل سياق السطر + الأسطر المجاورة
- `AgentReviewResponsePayload`: يستقبل قرار المراجعة (accept / reclassify / skip)

---

### 13. `src/types/external-modules.d.ts` — تعريفات وحدات خارجية

| الحقل          | القيمة                    |
| -------------- | ------------------------- |
| **الحالة**     | ✅ مفعّل (بنية تحتية)     |
| **الصادرات**   | تعريفات محيطة (ambient)   |
| **المستهلكون** | browser-extract.ts ضمنياً |

**الدور:**
يوفر أنواع TypeScript لمكتبات خارجية بدون تعريفات مدمجة:

- `pdfjs-dist`: استخراج نص من PDF

---

### 14. `src/types/index.ts` — ملف التجميع (Barrel)

| الحقل          | القيمة                               |
| -------------- | ------------------------------------ |
| **الحالة**     | ✅ مفعّل                             |
| **الصادرات**   | إعادة تصدير كل شيء من الملفات الأخرى |
| **المستهلكون** | كامل قاعدة الكود عبر `@/types`       |

**الدور:**
نقطة استيراد موحدة — يجمع كل الأنواع في مكان واحد لتسهيل الاستيراد:

```typescript
import { DocumentStats, LineType, TypingSystemSettings } from "@/types";
```

---

## ملفات الأدوات (Utils)

### 15. `src/utils/cn.ts` — دمج فئات CSS

| الحقل          | القيمة                            |
| -------------- | --------------------------------- |
| **الحالة**     | ✅ مفعّل                          |
| **الصادرات**   | `cn` (إعادة تصدير من `lib/utils`) |
| **المستهلكون** | مكونات UI (hover-border-gradient) |

**الدور:**
دالة مساعدة تدمج فئات Tailwind CSS بذكاء باستخدام `clsx` + `tailwind-merge`:

```typescript
cn("px-4 py-2", "px-6"); // → 'px-6 py-2' (يحل التعارضات)
```

---

### 16. `src/utils/logger.ts` — نظام السجلات

| الحقل          | القيمة                                  |
| -------------- | --------------------------------------- |
| **الحالة**     | ✅ مفعّل                                |
| **الصادرات**   | `logger`, `LogLevel`, `LogContext`      |
| **المستهلكون** | App.tsx, ScreenplayEditor.ts (3+ ملفات) |

**الدور:**
نظام تسجيل مركزي مع نطاقات وبيانات سياقية:

- `logger.info()` — رسائل معلوماتية (مثل: اكتمال استيراد ملف)
- `logger.error()` — أخطاء (مثل: فشل خط الاستيراد)
- `logger.debug()` — تصحيح أخطاء (بيئة التطوير فقط)

---

### 17. `src/utils/file-import/file-picker.ts` — محدد الملفات

| الحقل          | القيمة                       |
| -------------- | ---------------------------- |
| **الحالة**     | ✅ مفعّل                     |
| **الصادرات**   | `pickImportFile`             |
| **المستهلكون** | App.tsx, ScreenplayEditor.ts |

**الدور:**
ينشئ عنصر `<input type="file">` مخفي ويفتح نافذة اختيار الملفات:

```
المستخدم يضغط "فتح ملف" → pickImportFile() → نافذة الملفات → File | null
```

---

### 18. `src/utils/file-import/extract/index.ts` — منسّق الاستخراج

| الحقل          | القيمة                                              |
| -------------- | --------------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                            |
| **الصادرات**   | `extractImportedFile`, `ExtractImportedFileOptions` |
| **المستهلكون** | App.tsx, ScreenplayEditor.ts                        |

**الدور:**
المنسّق الرئيسي لاستخراج النص من الملفات — يوجّه حسب نوع الملف:

- `doc` → Backend فقط (يحتاج Antiword)
- `pdf` → المتصفح أولاً، ثم Backend إذا الجودة منخفضة
- باقي الأنواع → المتصفح أولاً، Backend كاحتياطي
- يطبق المعالجة الأولية وكشف الحمولة (payload)

---

### 19. `src/utils/file-import/extract/browser-extract.ts` — استخراج في المتصفح

| الحقل          | القيمة                                                 |
| -------------- | ------------------------------------------------------ |
| **الحالة**     | ✅ مفعّل                                               |
| **الصادرات**   | `isBrowserExtractionSupported`, `extractFileInBrowser` |
| **المستهلكون** | extract/index.ts                                       |

**الدور:**
يستخرج النص من الملفات مباشرة في المتصفح:

- **txt, fountain, fdx** → فك ترميز UTF-8 مع احتياطي للترميز
- **pdf** → مكتبة pdfjs-dist (طبقة النص فقط، بدون OCR)
- يكشف علامات الحمولة لاسترجاع الكتل المهيكلة

---

### 20. `src/utils/file-import/extract/backend-extract.ts` — استخراج عبر الخادم

| الحقل          | القيمة                                                                             |
| -------------- | ---------------------------------------------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                                                           |
| **الصادرات**   | `isBackendExtractionConfigured`, `extractFileWithBackend`, `BackendExtractOptions` |
| **المستهلكون** | extract/index.ts                                                                   |

**الدور:**
يرسل الملف كـ Base64 JSON لخادم REST API خلفي:

- مهلة افتراضية: 45 ثانية
- يُستخدم كاحتياطي لملفات DOC واستخراجات PDF منخفضة الجودة
- يدعم نقطة نهاية قابلة للتكوين

---

### 21. `src/utils/file-import/preprocessor.ts` — المعالج الأولي

| الحقل          | القيمة               |
| -------------- | -------------------- |
| **الحالة**     | ✅ مفعّل             |
| **الصادرات**   | 4 (3 دوال + 1 واجهة) |
| **المستهلكون** | extract/index.ts     |

**الدور:**
يعالج النص المستورد قبل التصنيف:

- دمج الأسطر الملتفة (wrapped lines)
- إزالة النقاط التعدادية
- تطبيع رؤوس المشاهد
- حساب نقاط جودة النص (0-1) بناءً على الشذوذات

---

### 22. `src/utils/file-import/document-model.ts` — نموذج المستند

| الحقل          | القيمة                                  |
| -------------- | --------------------------------------- |
| **الحالة**     | ✅ مفعّل                                |
| **الصادرات**   | 16 (10 دوال + 6 أنواع/ثوابت)            |
| **المستهلكون** | EditorArea, extract/\*, browser-extract |

**الدور:**
نواة نموذج المستند — يدير تحويل البيانات بين الأشكال المختلفة:

- `htmlToScreenplayBlocks()` — HTML → كتل سيناريو
- `screenplayBlocksToHtml()` — كتل → HTML
- `encodeScreenplayPayload()` / `decodeScreenplayPayload()` — ترميز/فك ترميز Base64
- `extractPayloadFromText()` — استخراج حمولة مشفرة من نص
- `ensurePayloadChecksum()` — إضافة checksum بخوارزمية FNV1a
- نمط `[[FILMLANE_PAYLOAD_V1:...]]` للحفاظ على البنية في الملفات النصية

---

### 23. `src/utils/file-import/structure-pipeline.ts` — خط أنابيب البنية

| الحقل          | القيمة                  |
| -------------- | ----------------------- |
| **الحالة**     | ✅ مفعّل                |
| **الصادرات**   | 4 دوال                  |
| **المستهلكون** | plain-text-to-blocks.ts |

**الدور:**
يحوّل النص الخام إلى كتل سيناريو مصنَّفة باستخدام آلة حالة (State Machine):

- `buildStructuredBlocksFromText()` — المحوّل الرئيسي
- يكتشف: رؤوس المشاهد، الحوارات، الحركة، الانتقالات بأنماط Regex
- `buildProjectionGuardReport()` — يتحقق أن النتيجة لا تنهار بنيوياً
- يدعم 3 سياسات دمج: none, safe, aggressive

---

### 24. `src/utils/file-import/plain-text-to-blocks.ts` — تحويل نص لكتل

| الحقل          | القيمة                               |
| -------------- | ------------------------------------ |
| **الحالة**     | ✅ مفعّل                             |
| **الصادرات**   | `plainTextToScreenplayBlocks`        |
| **المستهلكون** | barrel export (file-import/index.ts) |

**الدور:**
غلاف (wrapper) حول `buildStructuredBlocksFromText()`:

- يأخذ نصاً خاماً + سياسة اختيارية
- يُرجع مصفوفة كتل فقط (بدون بيانات وصفية)

---

### 25. `src/utils/file-import/open-pipeline.ts` — خط أنابيب الفتح

| الحقل          | القيمة                                                  |
| -------------- | ------------------------------------------------------- |
| **الحالة**     | ✅ مفعّل                                                |
| **الصادرات**   | `buildFileOpenPipelineAction`, `FileOpenPipelineAction` |
| **المستهلكون** | App.tsx, ScreenplayEditor.ts                            |

**الدور:**
يبني الإجراء النهائي بعد الاستخراج — يُرجع أحد 3 أنواع:

1. `import-structured-blocks` — إدراج كتل مباشرة (الملف يحتوي حمولة)
2. `import-classified-text` — النص يحتاج تصنيف (نص خام)
3. `reject` — الملف فارغ/غير صالح

---

### 26. `src/utils/file-import/index.ts` — تجميع أدوات الاستيراد

| الحقل          | القيمة                             |
| -------------- | ---------------------------------- |
| **الحالة**     | ✅ مفعّل                           |
| **الصادرات**   | إعادة تصدير من 7 وحدات فرعية       |
| **المستهلكون** | 9+ ملفات عبر `@/utils/file-import` |

**الدور:**
نقطة استيراد موحدة لكل وظائف استيراد الملفات.

---

## جدول التفعيل الشامل

| #   | الملف                                    | الحالة       | الدور                    | المستهلكون           |
| --- | ---------------------------------------- | ------------ | ------------------------ | -------------------- |
| 1   | `main.tsx`                               | ✅ مفعّل     | نقطة دخول التطبيق        | index.html           |
| 2   | `App.tsx`                                | ✅ مفعّل     | المكوّن الجذري + UI كامل | main.tsx             |
| 3   | `editor.ts`                              | ✅ مفعّل     | مصنع محرر TipTap         | EditorArea +4        |
| 4   | `toolbar.ts`                             | ❌ غير مفعّل | شريط أدوات DOM قديم      | لا أحد               |
| 5   | `vite-env.d.ts`                          | ✅ مفعّل     | تعريفات Vite             | TypeScript           |
| 6   | `types/screenplay.ts`                    | ✅ مفعّل     | أنواع السيناريو الأساسية | 6+ ملفات             |
| 7   | `types/file-import.ts`                   | ✅ مفعّل     | أنواع استيراد الملفات    | 10+ ملفات            |
| 8   | `types/editor-clipboard.ts`              | ✅ مفعّل     | حافظة مخصصة              | 7+ استخدامات         |
| 9   | `types/editor-engine.ts`                 | ✅ مفعّل     | واجهة محرك المحرر        | App.tsx              |
| 10  | `types/structure-pipeline.ts`            | ✅ مفعّل     | سياسات خط أنابيب البنية  | 8+ استخدامات         |
| 11  | `types/typing-system.ts`                 | ✅ مفعّل     | نظام الكتابة التلقائي    | App.tsx              |
| 12  | `types/agent-review.ts`                  | ✅ مفعّل     | مراجعة الوكيل الذكي      | paste-classifier     |
| 13  | `types/external-modules.d.ts`            | ✅ مفعّل     | تعريفات مكتبات خارجية    | browser-extract      |
| 14  | `types/index.ts`                         | ✅ مفعّل     | تجميع الأنواع            | كامل الكود           |
| 15  | `utils/cn.ts`                            | ✅ مفعّل     | دمج فئات CSS             | مكونات UI            |
| 16  | `utils/logger.ts`                        | ✅ مفعّل     | نظام سجلات مركزي         | App +2               |
| 17  | `file-import/file-picker.ts`             | ✅ مفعّل     | محدد الملفات             | App +1               |
| 18  | `file-import/extract/index.ts`           | ✅ مفعّل     | منسّق الاستخراج          | App +1               |
| 19  | `file-import/extract/browser-extract.ts` | ✅ مفعّل     | استخراج متصفح            | extract/index        |
| 20  | `file-import/extract/backend-extract.ts` | ✅ مفعّل     | استخراج خادم             | extract/index        |
| 21  | `file-import/preprocessor.ts`            | ✅ مفعّل     | معالجة أولية             | extract/index        |
| 22  | `file-import/document-model.ts`          | ✅ مفعّل     | نموذج المستند            | EditorArea +3        |
| 23  | `file-import/structure-pipeline.ts`      | ✅ مفعّل     | تحويل نص → كتل           | plain-text-to-blocks |
| 24  | `file-import/plain-text-to-blocks.ts`    | ✅ مفعّل     | غلاف التحويل             | barrel export        |
| 25  | `file-import/open-pipeline.ts`           | ✅ مفعّل     | خط أنابيب الفتح          | App +1               |
| 26  | `file-import/index.ts`                   | ✅ مفعّل     | تجميع أدوات الاستيراد    | 9+ ملفات             |

---

## مقارنة مع المنافسين

### المنافسون المرجعيون:

1. **Final Draft** — المعيار الصناعي ($250، سطح المكتب)
2. **Celtx** — سحابي مجاني/مدفوع
3. **Highland 2** — macOS ($50)
4. **Fade In** — متعدد المنصات ($80)
5. **WriterSolo** — مجاني/مفتوح المصدر (ويب)
6. **KIT Scenarist** — مجاني/مفتوح المصدر (سطح مكتب)
7. **Fountain** — تنسيق نص عادي مفتوح

---

### أ) ميزات نحن أكثر تطوراً فيها 🟢

| الميزة                             | أفان تيتر                                   | المنافسون                                     |
| ---------------------------------- | ------------------------------------------- | --------------------------------------------- |
| **دعم اللغة العربية الأصلي**       | RTL أصلي من الجذر، خطوط عربية، بسملة كعنصر  | معظمهم يدعمون العربية كلغة ثانوية أو بدون دعم |
| **تصنيف النص بالذكاء الاصطناعي**   | `PasteClassifier` + `AgentReview` مع Claude | لا يوجد عند أي منافس مفتوح المصدر             |
| **نظام كتابة تلقائي 3 أوضاع**      | plain / auto-deferred / auto-live           | معظمهم يدوي فقط أو auto فقط                   |
| **خط أنابيب استيراد مزدوج**        | متصفح + خادم مع fallback ذكي وجودة          | معظمهم يدعم طريقة واحدة فقط                   |
| **حافظة مخصصة بـ MIME**            | تحافظ على التصنيف عند النسخ/اللصق           | معظم المحررات تفقد التصنيف                    |
| **حارس الجودة (Projection Guard)** | يمنع الكتابة فوق البنية بنتائج أسوأ         | ميزة فريدة                                    |
| **ترميز حمولة مع checksum**        | FNV1a checksum + Base64 في الملفات النصية   | لا يوجد مكافئ                                 |
| **عنصر البسملة**                   | عنصر هيكلي أصلي                             | غير موجود في أي محرر آخر                      |

---

### ب) ميزات المنافسون أكثر تطوراً فيها 🔴

| الميزة                     | المنافسون                                              | أفان تيتر                |
| -------------------------- | ------------------------------------------------------ | ------------------------ |
| **التصدير المتعدد**        | Final Draft: PDF, FDX, RTF, Fountain, HTML, Word       | HTML فقط حالياً          |
| **التعاون الفوري**         | Celtx: تعاون سحابي مباشر                               | محرر فردي فقط            |
| **إدارة المشاهد**          | Final Draft/Celtx: إعادة ترتيب سحب-إفلات، بطاقات مشاهد | لا يوجد                  |
| **التحكم بالمراجعات**      | Final Draft: ألوان مراجعة، تتبع تغييرات                | لا يوجد                  |
| **فهرسة شخصيات**           | Final Draft/Celtx: قائمة شخصيات مع ملاحظات             | لا يوجد                  |
| **إحصائيات متقدمة**        | Final Draft: تقدير وقت القراءة، إحصائيات بالمشهد       | إحصائيات عامة فقط        |
| **محرك بحث/استبدال**       | كل المحررات التجارية                                   | غير مرئي في الكود الحالي |
| **قوالب مسبقة**            | Celtx/Highland: قوالب حسب النوع                        | لا يوجد نظام قوالب       |
| **تطبيقات الهاتف**         | Celtx/Final Draft: iOS/Android                         | ويب فقط                  |
| **وضع عدم الاتصال الكامل** | Highland/KIT Scenarist: تطبيق سطح مكتب                 | يعتمد جزئياً على خادم    |

---

### ج) ميزات متساوية تقريباً 🟡

| الميزة                           | التفاصيل                                       |
| -------------------------------- | ---------------------------------------------- |
| **ترقيم صفحات A4**               | مثل Final Draft — Pages extension مع رأس/تذييل |
| **8 أنواع عناصر سيناريو**        | نفس العدد تقريباً عند كل المحررات الكبرى       |
| **اختصارات لوحة المفاتيح**       | Ctrl+0-7 — مشابه لـ Final Draft/Fade In        |
| **تنسيق (غامق/مائل/تسطير)**      | موجود عند الجميع                               |
| **استيراد ملفات متعددة الأنواع** | 5 أنواع (doc, txt, pdf, fountain, fdx) — مشابه |
| **دعم Fountain**                 | مثل Highland/WriterSolo                        |
| **وضع داكن**                     | مثل Highland 2 / WriterSolo                    |

---

### د) ميزات غير موجودة عندنا أصلاً 🔵

| الميزة                     | من يملكها            | الوصف                                    |
| -------------------------- | -------------------- | ---------------------------------------- |
| **Storyboard / لوحة قصة**  | Celtx                | رسم مشاهد بصرياً مع ربطها بالسيناريو     |
| **نظام إنتاج متكامل**      | Celtx, Final Draft   | جداول تصوير، ميزانيات، تقارير إنتاج      |
| **تحويل نص لكلام (TTS)**   | Highland 2           | قراءة السيناريو صوتياً لمراجعته          |
| **Big Board / لوحة كبيرة** | Final Draft          | عرض كل المشاهد كبطاقات على لوحة واحدة    |
| **ملاحظات مرتبطة بالنص**   | Final Draft, Fade In | تعليقات وملاحظات على أسطر محددة          |
| **مقارنة إصدارات**         | Final Draft          | مقارنة نسختين من السيناريو جنب لجنب      |
| **تكامل مع أدوات إنتاج**   | Celtx                | ربط مع Scheduling, Budgeting, Shot Lists |
| **صفحة عنوان مخصصة**       | كلهم تقريباً         | صفحة أولى بعنوان + مؤلف + معلومات        |
| **تقسيم الفصول**           | Final Draft          | فصول / أجزاء داخل السيناريو              |
| **وضع Beat Board**         | Final Draft          | تخطيط إيقاع القصة قبل الكتابة            |

---

## توصيات

### إجراءات فورية:

1. **حذف `toolbar.ts`** أو نقله لمجلد `archive/` — كود ميت لا يُستخدم
2. **إزالة استيراد `toolbar.css`** من `main.tsx` إذا لم تكن هناك أنماط مشتركة

### تحسينات قصيرة المدى:

3. **إضافة تصدير PDF** — أهم ميزة ناقصة
4. **صفحة عنوان** — متوقعة في أي محرر سيناريو
5. **بحث واستبدال** — ميزة أساسية مفقودة
6. **فهرسة الشخصيات** — تستخرج من الحوارات الموجودة

### تحسينات متوسطة المدى:

7. **إدارة المشاهد** بالسحب والإفلات
8. **تتبع المراجعات** بألوان
9. **تصدير FDX** للتوافق مع Final Draft
10. **وضع عدم اتصال كامل** باستخدام Service Worker

---

> **الخلاصة:** المشروع يتفوق بشكل واضح في دعم العربية الأصلي والتصنيف بالذكاء الاصطناعي ونظام الحافظة الذكي. نقاط الضعف الرئيسية هي في التصدير المتعدد وأدوات ما بعد الكتابة (إنتاج، مراجعات، تعاون).
