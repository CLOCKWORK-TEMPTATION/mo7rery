import {
  runOpenPipelineHarness,
  runPlainTextBlocksHarness,
  runStructurePipelineHarness,
} from "./pipeline-harness";

runStructurePipelineHarness();
runPlainTextBlocksHarness();
runOpenPipelineHarness();
